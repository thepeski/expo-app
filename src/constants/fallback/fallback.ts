/* @constants/fallback/fallback.ts */
// fallback screen messages

const fallback = {
    loadingMsg: "Loading...",
    errorMsg: "We couldn't load the app right now",
    action: "Reload",
} as const;

export { fallback };