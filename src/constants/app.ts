/* @constants/app.ts */
// app menu list

const app = [
    {
        category: "test",
        groups: [
            {
                group: "test",
                routes: [{ label: "test", path: "test" }]
            }
        ]
    }
]

export default app;