/* Theme.js */
// colour theme names & modes

const Theme = {
    name: {
        default: "light",
        light: "light",
        dark: "dark"
    },
    mode: {
        user: "user",
        system: "system"
    }
}

export default Theme;