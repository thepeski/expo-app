/* FallbackNullTest.tsx */
// empty fallback screen test

// src imports
import { Fallback } from "@screens";

function FallbackNullTest() {
    return (
        <Fallback />
    );
}

export default FallbackNullTest;